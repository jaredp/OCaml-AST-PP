Usage:
ast-pp "Ast" < ast.ml > pp.ml
where Ast is the name of the module ast.ml, which is the module containing the structures you want to pretty-print and pp.ml is the file you want the pretty printer to be in.

In pp.ml then is a function in the form of
let string_of_xyz obj = ...
for each type xyz defined in ast.ml

Installation:
Run make while in build/ to generate the ast-pp executable.
You may move it into a directory in your path if you like, otherwise you must call it with the full path to the executable.
